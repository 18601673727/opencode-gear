{
  "$comment": "Example PROJECT override. Copy to <project>/.opencode-gear.json and keep only the keys you need. This file is read automatically when `ocg` runs in that project directory, and is deep-merged over the gear defaults and over your user config. Nothing here belongs in the OpenCode Gear repository itself.",
  "throttle": {
    "default": "mid"
  },
  "routing": {
    "roles": {
      "build": { "model": "glm-5.3", "variant": "max" },
      "verify": { "model": "glm-5.3-flash", "variant": "high" }
    }
  },
  "prompts": {
    "lead": { "append": [".opencode/lead-policy.md"] }
  }
}
